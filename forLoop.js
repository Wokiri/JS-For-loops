for (let numbers = 5; numbers <= 12; numbers += 1) {
    console.log(numbers)
}

// Numbers += 1 may also be written as numbers ++
// They both mean an increment of 1 in every iteration
// An increment of 2 would be numbers += 2

// The variable 'numbers' is accessible within the loop block